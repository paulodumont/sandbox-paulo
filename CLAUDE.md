# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is a personal sandbox repository ("My own sandbox") used for experimentation. It currently contains no application code, build system, or test suite — only this file and the README.

## Current State

- No package manifests, build tooling, linters, or test frameworks are configured yet.
- There are no build, lint, or test commands to run at this time.
- The default branch is `main`.

## Working in This Repository

- Since this is a sandbox, expect experiments to be self-contained. When adding a new project or experiment, include its own tooling configuration (e.g., a package manifest) alongside it.
- When code with a build system, tests, or a linter is added, update this file with the relevant commands (build, lint, test, and how to run a single test) and a description of the architecture.
